source "https://rubygems.org"

# Matches the exact gem versions GitHub Pages runs, so local == production.
gem "github-pages", group: :jekyll_plugins

# Needed on Ruby 3+ for `jekyll serve`
gem "webrick", "~> 1.8"
